/**
 * 配置模块
 * 
 * @module drunk.config
 * @class config
 */

module drunk.config {
    
    /**
     * 绑定指令的前缀
     * 
     * @property prefix
     * @type string
     */
    export var prefix: string = "drunk-";
    
    /**
     * debug模式配置变量
     * 
     * @property debug
     * @type boolean
     */
    export var debug: boolean = true;
}